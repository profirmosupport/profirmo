export const metadata = {
  title: 'Dashboard',
  description:
    'Manage your consultations, cases, clients and account on Pro Firmo.',
  robots: { index: false, follow: false },
};

export default function DashboardSegmentLayout({ children }) {
  return children;
}
