export const metadata = {
  title: 'Consultation Room',
  description: 'Your live online consultation room on Pro Firmo.',
  robots: { index: false, follow: false },
};

export default function ConsultationLayout({ children }) {
  return children;
}
